# FaceIt 接口说明书

## 一、接口概述

### 1.1 基础信息

**基础路径**：`/api/v1`
**认证方式**：JWT Token（Header: Authorization: Bearer {token}）
**响应格式**：JSON
**字符编码**：UTF-8

### 1.2 统一响应结构

```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

### 1.3 状态码说明

| 状态码 | 说明 |
| :--- | :--- |
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权（Token 无效或过期） |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

---

## 二、认证接口

### 2.1 用户注册

**接口路径**：`POST /api/v1/auth/register`
**是否需要认证**：否

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| username | string | 是 | 用户名（3-50字符） |
| password | string | 是 | 密码（6-20字符） |
| email | string | 否 | 邮箱 |
| phone | string | 否 | 手机号 |

**请求示例**：

```json
{
  "username": "testuser",
  "password": "Test123456",
  "email": "test@example.com"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "注册成功",
  "data": null
}
```

---

### 2.2 用户登录

**接口路径**：`POST /api/v1/auth/login`
**是否需要认证**：否

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| username | string | 是 | 用户名 |
| password | string | 是 | 密码 |

**请求示例**：

```json
{
  "username": "testuser",
  "password": "Test123456"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "userId": 1,
    "username": "testuser",
    "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "expiresIn": 86400
  }
}
```

---

### 2.3 用户登出

**接口路径**：`POST /api/v1/auth/logout`
**是否需要认证**：是

**请求参数**：无

**响应示例**：

```json
{
  "code": 200,
  "message": "登出成功",
  "data": null
}
```

---

## 三、用户接口

### 3.1 获取用户信息

**接口路径**：`GET /api/v1/user/profile`
**是否需要认证**：是

**请求参数**：无

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "username": "testuser",
    "email": "test@example.com",
    "phone": "13800138000",
    "realName": "张三",
    "school": "清华大学",
    "major": "计算机科学与技术",
    "grade": "大四",
    "targetPosition": "java_backend",
    "jobStatus": "preparing",
    "avatarUrl": "https://example.com/avatar.jpg",
    "createdAt": "2026-03-01T10:00:00"
  }
}
```

---

### 3.2 更新用户信息

**接口路径**：`PUT /api/v1/user/profile`
**是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| realName | string | 否 | 真实姓名 |
| school | string | 否 | 学校 |
| major | string | 否 | 专业 |
| grade | string | 否 | 年级 |
| targetPosition | string | 否 | 意向岗位：java_backend/web_frontend |
| jobStatus | string | 否 | 求职阶段：preparing/interviewing/offered |
| avatarUrl | string | 否 | 头像URL |

**请求示例**：

```json
{
  "realName": "张三",
  "school": "清华大学",
  "major": "计算机科学与技术",
  "grade": "大四",
  "targetPosition": "java_backend"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

---

### 3.3 修改密码

**接口路径**：`PUT /api/v1/user/password`
**是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| oldPassword | string | 是 | 原密码 |
| newPassword | string | 是 | 新密码（6-20字符） |

**请求示例**：

```json
{
  "oldPassword": "OldPassword123",
  "newPassword": "NewPassword456"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "修改成功",
  "data": null
}
```

---

## 四、面试接口

### 4.1 创建面试会话

**接口路径**：`POST /api/v1/interview/create`
**是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| jobPosition | string | 是 | 面试岗位：java_backend/web_frontend |
| interviewMode | string | 是 | 面试模式：basic/full/special |
| durationMinutes | integer | 是 | 面试时长：10/20/30（分钟） |

**请求示例**：

```json
{
  "jobPosition": "java_backend",
  "interviewMode": "full",
  "durationMinutes": 30
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "userId": 1,
    "jobPosition": "java_backend",
    "interviewMode": "full",
    "durationMinutes": 30,
    "status": "pending",
    "createdAt": "2026-03-01T10:00:00"
  }
}
```

---

### 4.2 开始面试

**接口路径**：`POST /api/v1/interview/{id}/start`
**是否需要认证**：是

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | integer | 是 | 面试会话ID |

**响应示例**：

```json
{
  "code": 200,
  "message": "面试已开始",
  "data": null
}
```

---

### 4.3 结束面试

**接口路径**：`POST /api/v1/interview/{id}/end`
**是否需要认证**：是

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | integer | 是 | 面试会话ID |

**响应示例**：

```json
{
  "code": 200,
  "message": "面试已结束",
  "data": null
}
```

---

### 4.4 获取面试详情

**接口路径**：`GET /api/v1/interview/{id}`
**是否需要认证**：是

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | integer | 是 | 面试会话ID |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "userId": 1,
    "jobPosition": "java_backend",
    "interviewMode": "full",
    "durationMinutes": 30,
    "status": "completed",
    "startTime": "2026-03-01T10:00:00",
    "endTime": "2026-03-01T10:30:00",
    "actualDuration": 1800,
    "totalQuestions": 15,
    "totalAnswers": 15,
    "createdAt": "2026-03-01T10:00:00"
  }
}
```

---

### 4.5 发送消息

**接口路径**：`POST /api/v1/interview/{id}/message`
**是否需要认证**：是

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | integer | 是 | 面试会话ID |

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| messageType | string | 是 | 消息类型：text/audio |
| content | string | 是 | 消息内容 |
| audioUrl | string | 否 | 音频URL（messageType为audio时） |
| audioDuration | number | 否 | 音频时长（秒） |

**请求示例**：

```json
{
  "messageType": "text",
  "content": "你好，我是来面试 Java 后端开发岗位的。"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "sessionId": 1,
    "role": "candidate",
    "messageType": "text",
    "content": "你好，我是来面试 Java 后端开发岗位的。",
    "stage": "self_intro",
    "sequence": 1,
    "createdAt": "2026-03-01T10:01:00"
  }
}
```

---

### 4.6 获取对话历史

**接口路径**：`GET /api/v1/interview/{id}/messages`
**是否需要认证**：是

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | integer | 是 | 面试会话ID |

**查询参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| page | integer | 否 | 页码，默认1 |
| pageSize | integer | 否 | 每页数量，默认20 |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 30,
    "page": 1,
    "pageSize": 20,
    "list": [
      {
        "id": 1,
        "sessionId": 1,
        "role": "interviewer",
        "messageType": "text",
        "content": "你好，欢迎参加今天的面试...",
        "stage": "self_intro",
        "sequence": 1,
        "createdAt": "2026-03-01T10:00:30"
      },
      {
        "id": 2,
        "sessionId": 1,
        "role": "candidate",
        "messageType": "text",
        "content": "你好，我是来面试 Java 后端开发岗位的。",
        "stage": "self_intro",
        "sequence": 2,
        "createdAt": "2026-03-01T10:01:00"
      }
    ]
  }
}
```

---

### 4.7 获取面试历史

**接口路径**：`GET /api/v1/interview/history`
**是否需要认证**：是

**查询参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| page | integer | 否 | 页码，默认1 |
| pageSize | integer | 否 | 每页数量，默认10 |
| jobPosition | string | 否 | 岗位筛选 |
| status | string | 否 | 状态筛选 |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 5,
    "page": 1,
    "pageSize": 10,
    "list": [
      {
        "id": 1,
        "userId": 1,
        "jobPosition": "java_backend",
        "interviewMode": "full",
        "durationMinutes": 30,
        "status": "completed",
        "startTime": "2026-03-01T10:00:00",
        "endTime": "2026-03-01T10:30:00",
        "actualDuration": 1800,
        "totalQuestions": 15,
        "totalAnswers": 15,
        "createdAt": "2026-03-01T10:00:00"
      }
    ]
  }
}
```

---

## 五、评估接口

### 5.1 生成评估报告

**接口路径**：`POST /api/v1/evaluation/generate`
**是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| sessionId | integer | 是 | 面试会话ID |

**请求示例**：

```json
{
  "sessionId": 1
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "评估报告生成成功",
  "data": {
    "id": 1,
    "sessionId": 1,
    "userId": 1,
    "overallScore": 85.5,
    "grade": "A",
    "technicalAccuracy": 88.0,
    "logicalRigor": 82.0,
    "jobMatching": 90.0,
    "expressionAbility": 85.0,
    "confidence": 80.0,
    "strengths": ["技术基础扎实", "表达清晰", "逻辑性强"],
    "weaknesses": ["分布式系统理解不足"],
    "summary": "整体表现优秀，技术基础扎实，表达清晰有条理...",
    "suggestions": ["深入学习 Redis 集群", "准备更多项目案例", "加强系统设计能力"],
    "radarChartData": {
      "dimensions": ["技术正确性", "逻辑严谨性", "岗位匹配度", "表达能力", "自信度"],
      "values": [88, 82, 90, 85, 80]
    },
    "createdAt": "2026-03-01T11:00:00"
  }
}
```

---

### 5.2 获取评估报告

**接口路径**：`GET /api/v1/evaluation/{id}`
**是否需要认证**：是

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | integer | 是 | 评估报告ID |

**响应示例**：同 5.1

---

### 5.3 获取逐题评价

**接口路径**：`GET /api/v1/evaluation/{id}/questions`
**是否需要认证**：是

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | integer | 是 | 评估报告ID |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "reportId": 1,
      "questionContent": "请介绍一下 Spring Boot 的自动装配原理",
      "answerContent": "Spring Boot 的自动装配是通过 @EnableAutoConfiguration 注解实现的...",
      "score": 85.0,
      "correctness": "correct",
      "highlights": ["理解核心原理", "提到关键注解"],
      "issues": ["可以更深入讲解 SPI 机制"],
      "improvement": "建议深入学习 Spring Factories 机制"
    }
  ]
}
```

---

### 5.4 获取评估历史

**接口路径**：`GET /api/v1/evaluation/history`
**是否需要认证**：是

**查询参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| page | integer | 否 | 页码，默认1 |
| pageSize | integer | 否 | 每页数量，默认10 |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 3,
    "page": 1,
    "pageSize": 10,
    "list": [
      {
        "id": 1,
        "sessionId": 1,
        "overallScore": 85.5,
        "grade": "A",
        "createdAt": "2026-03-01T11:00:00"
      }
    ]
  }
}
```

---

## 六、知识库接口

### 6.1 检索知识库

**接口路径**：`POST /api/v1/knowledge/search`
**是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| query | string | 是 | 查询内容 |
| jobPosition | string | 是 | 岗位：java_backend/web_frontend |
| topK | integer | 否 | 返回数量，默认5 |

**请求示例**：

```json
{
  "query": "Spring Boot 自动装配原理",
  "jobPosition": "java_backend",
  "topK": 5
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "results": [
      {
        "chunkId": 1,
        "documentId": 1,
        "content": "Spring Boot 自动装配是通过 @EnableAutoConfiguration...",
        "score": 0.95,
        "title": "Spring Boot 核心原理"
      }
    ]
  }
}
```

---

### 6.2 获取知识库列表

**接口路径**：`GET /api/v1/knowledge/list`
**是否需要认证**：是

**查询参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| jobPosition | string | 否 | 岗位筛选 |
| category | string | 否 | 分类筛选 |
| page | integer | 否 | 页码，默认1 |
| pageSize | integer | 否 | 每页数量，默认20 |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 50,
    "page": 1,
    "pageSize": 20,
    "list": [
      {
        "id": 1,
        "jobPosition": "java_backend",
        "category": "tech_stack",
        "title": "Spring Boot 核心原理",
        "tags": "Spring Boot,自动装配",
        "difficulty": "intermediate",
        "createdAt": "2026-03-01T10:00:00"
      }
    ]
  }
}
```

---

### 6.3 获取知识库文档详情

**接口路径**：`GET /api/v1/knowledge/{id}`
**是否需要认证**：是

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | integer | 是 | 文档ID |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "jobPosition": "java_backend",
    "category": "tech_stack",
    "title": "Spring Boot 核心原理",
    "content": "# Spring Boot 核心原理\n\n## 自动装配\n...",
    "tags": "Spring Boot,自动装配",
    "difficulty": "intermediate",
    "createdAt": "2026-03-01T10:00:00",
    "updatedAt": "2026-03-01T10:00:00"
  }
}
```

---

### 6.4 上传知识文档

**接口路径**：`POST /api/v1/knowledge/upload`
**是否需要认证**：是
**Content-Type**：multipart/form-data

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| file | file | 是 | 文档文件（支持 .md, .txt, .pdf） |
| jobPosition | string | 是 | 岗位 |
| category | string | 是 | 分类 |
| title | string | 否 | 标题 |
| tags | string | 否 | 标签（逗号分隔） |
| difficulty | string | 否 | 难度：basic/intermediate/advanced |

**响应示例**：

```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "id": 1,
    "title": "Spring Boot 核心原理"
  }
}
```

---

## 七、统计接口

### 7.1 获取成长曲线

**接口路径**：`GET /api/v1/stats/growth-curve`
**是否需要认证**：是

**查询参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| jobPosition | string | 否 | 岗位筛选 |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "labels": ["2026-03-01", "2026-03-05", "2026-03-10"],
    "overallScores": [75.0, 80.0, 85.5],
    "technicalAccuracy": [70.0, 78.0, 88.0],
    "logicalRigor": [72.0, 80.0, 82.0],
    "jobMatching": [80.0, 82.0, 90.0],
    "expressionAbility": [75.0, 78.0, 85.0],
    "confidence": [70.0, 75.0, 80.0]
  }
}
```

---

### 7.2 获取雷达图数据

**接口路径**：`GET /api/v1/stats/radar-chart`
**是否需要认证**：是

**查询参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| jobPosition | string | 否 | 岗位筛选 |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "dimensions": ["技术正确性", "逻辑严谨性", "岗位匹配度", "表达能力", "自信度"],
    "current": [88, 82, 90, 85, 80],
    "average": [75, 70, 78, 72, 68]
  }
}
```

---

### 7.3 获取薄弱环节

**接口路径**：`GET /api/v1/stats/weak-points`
**是否需要认证**：是

**查询参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| jobPosition | string | 否 | 岗位筛选 |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "weakPoints": [
      {
        "dimension": "技术正确性",
        "score": 65.0,
        "description": "分布式系统相关知识薄弱"
      },
      {
        "dimension": "自信度",
        "score": 70.0,
        "description": "回答时略显紧张，建议多练习"
      }
    ]
  }
}
```

---

### 7.4 获取推荐练习

**接口路径**：`GET /api/v1/stats/recommendations`
**是否需要认证**：是

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "recommendationType": "knowledge",
      "title": "深入学习 Redis 集群",
      "description": "根据评估结果，建议加强 Redis 集群相关知识",
      "resourceUrl": "https://example.com/redis-cluster",
      "priority": 1,
      "status": "pending"
    },
    {
      "id": 2,
      "recommendationType": "question",
      "title": "练习分布式系统设计题",
      "description": "多练习分布式场景的设计题目",
      "resourceUrl": null,
      "priority": 2,
      "status": "pending"
    }
  ]
}
```

---

### 7.5 更新推荐状态

**接口路径**：`PUT /api/v1/stats/recommendations/{id}`
**是否需要认证**：是

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | integer | 是 | 推荐ID |

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| status | string | 是 | 状态：pending/completed/ignored |

**请求示例**：

```json
{
  "status": "completed"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

---

## 八、AI 接口

### 8.1 AI 对话

**接口路径**：`POST /api/v1/ai/chat`
**是否需要认证**：是
**响应类型**：text/event-stream（SSE 流式响应）

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| sessionId | integer | 是 | 面试会话ID |
| message | string | 是 | 用户消息 |
| messageType | string | 否 | 消息类型，默认 text |

**请求示例**：

```json
{
  "sessionId": 1,
  "message": "你好，我是来面试 Java 后端开发岗位的。",
  "messageType": "text"
}
```

**响应示例**（SSE 流式）：

```
data: {"content":"你好"}

data: {"content":"，欢迎"}

data: {"content":"参加今天的面试"}

data: {"content":"。请先"}

data: {"content":"简单介绍一下"}

data: {"content":"你自己。"}

data: [DONE]
```

---

### 8.2 AI 评估

**接口路径**：`POST /api/v1/ai/evaluate`
**是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| sessionId | integer | 是 | 面试会话ID |

**请求示例**：

```json
{
  "sessionId": 1
}
```

**响应示例**：同 5.1 评估报告

---

## 九、音频接口

### 9.1 语音识别（ASR）

**接口路径**：`POST /api/v1/audio/asr`
**是否需要认证**：是
**Content-Type**：multipart/form-data

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| file | file | 是 | 音频文件（支持 .wav, .mp3, .webm） |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "text": "你好，我是来面试 Java 后端开发岗位的。",
    "duration": 3.5
  }
}
```

---

### 9.2 语音合成（TTS）

**接口路径**：`POST /api/v1/audio/tts`
**是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| text | string | 是 | 要合成的文本 |
| voiceType | string | 否 | 声音类型：male/female，默认 male |

**请求示例**：

```json
{
  "text": "你好，欢迎参加面试。",
  "voiceType": "male"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "audioUrl": "https://example.com/audio/123.mp3",
    "duration": 2.5
  }
}
```

---

## 十、WebSocket 接口

### 10.1 面试实时通信

**连接地址**：`ws://localhost:8080/ws/interview/{sessionId}`
**认证方式**：URL 参数 `?token={accessToken}`

**消息格式**：

```json
{
  "type": "message",
  "data": {
    "content": "用户消息内容",
    "messageType": "text"
  }
}
```

**服务端推送格式**：

```json
{
  "type": "ai_response",
  "data": {
    "content": "AI 回复内容",
    "isEnd": false
  }
}
```

---

## 十一、岗位配置接口

### 11.1 获取岗位列表

**接口路径**：`GET /api/v1/positions`
**是否需要认证**：否

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "code": "java_backend",
      "name": "Java 后端开发",
      "description": "负责后端服务开发、数据库设计、接口开发等",
      "icon": "java",
      "status": 1
    },
    {
      "code": "web_frontend",
      "name": "Web 前端开发",
      "description": "负责前端界面开发、用户交互、性能优化等",
      "icon": "frontend",
      "status": 1
    }
  ]
}
```

---

### 11.2 获取岗位配置

**接口路径**：`GET /api/v1/positions/{code}/config`
**是否需要认证**：否

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| code | string | 是 | 岗位代码：java_backend/web_frontend |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "code": "java_backend",
    "name": "Java 后端开发",
    "description": "负责后端服务开发、数据库设计、接口开发等",
    "techStack": [
      {"name": "Java", "level": "核心"},
      {"name": "Spring Boot", "level": "核心"},
      {"name": "MySQL", "level": "核心"},
      {"name": "Redis", "level": "重要"},
      {"name": "Spring Cloud", "level": "重要"},
      {"name": "Docker", "level": "了解"}
    ],
    "examPoints": [
      "Java 基础与集合",
      "并发编程与 JVM",
      "Spring 框架原理",
      "数据库设计与优化",
      "分布式系统设计",
      "项目经验与架构"
    ],
    "dimensions": [
      {"name": "技术正确性", "weight": 0.30, "description": "知识点是否准确"},
      {"name": "逻辑严谨性", "weight": 0.25, "description": "表达是否有条理"},
      {"name": "岗位匹配度", "weight": 0.20, "description": "与岗位要求契合度"},
      {"name": "表达能力", "weight": 0.15, "description": "语速、清晰度、流畅度"},
      {"name": "自信度", "weight": 0.10, "description": "语音情感分析"}
    ],
    "interviewModes": [
      {"code": "basic", "name": "基础模式", "description": "基础题+简单流程"},
      {"code": "full", "name": "全真模式", "description": "完整企业面试流程+压力追问"},
      {"code": "special", "name": "专项训练", "description": "技术题/项目题/行为题"}
    ]
  }
}
```

---

## 十二、练习计划接口

### 12.1 获取练习计划

**接口路径**：`GET /api/v1/practice/plans`
**是否需要认证**：是

**查询参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| jobPosition | string | 否 | 岗位筛选 |
| status | string | 否 | 状态筛选：active/completed/cancelled |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "userId": 1,
      "jobPosition": "java_backend",
      "title": "Java 并发编程强化计划",
      "description": "根据评估结果，建议加强并发编程相关知识",
      "planData": {
        "tasks": [
          {"day": 1, "topic": "线程基础", "questions": 5},
          {"day": 2, "topic": "锁机制", "questions": 5},
          {"day": 3, "topic": "并发容器", "questions": 5},
          {"day": 4, "topic": "线程池", "questions": 5},
          {"day": 5, "topic": "综合练习", "questions": 10}
        ]
      },
      "startDate": "2026-03-20",
      "endDate": "2026-03-25",
      "status": "active",
      "progress": 40,
      "createdAt": "2026-03-19T10:00:00"
    }
  ]
}
```

---

### 12.2 生成练习计划

**接口路径**：`POST /api/v1/practice/plans`
**是否需要认证**：是

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| jobPosition | string | 是 | 岗位代码 |
| reportId | integer | 否 | 基于指定报告生成 |
| duration | integer | 否 | 计划天数，默认7天 |

**请求示例**：

```json
{
  "jobPosition": "java_backend",
  "reportId": 1,
  "duration": 7
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "练习计划生成成功",
  "data": {
    "id": 1,
    "title": "Java 并发编程强化计划",
    "startDate": "2026-03-20",
    "endDate": "2026-03-27",
    "totalTasks": 7
  }
}
```

---

### 12.3 更新练习计划进度

**接口路径**：`PUT /api/v1/practice/plans/{id}/progress`
**是否需要认证**：是

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | integer | 是 | 计划ID |

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| day | integer | 是 | 完成的天数 |
| completed | boolean | 是 | 是否完成 |

**请求示例**：

```json
{
  "day": 1,
  "completed": true
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "进度更新成功",
  "data": {
    "progress": 20
  }
}
```

---

## 十三、错题本接口

### 13.1 获取错题列表

**接口路径**：`GET /api/v1/practice/mistakes`
**是否需要认证**：是

**查询参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| jobPosition | string | 否 | 岗位筛选 |
| page | integer | 否 | 页码，默认1 |
| pageSize | integer | 否 | 每页数量，默认20 |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 15,
    "page": 1,
    "pageSize": 20,
    "list": [
      {
        "id": 1,
        "userId": 1,
        "jobPosition": "java_backend",
        "questionContent": "请解释 Java 中的 volatile 关键字的作用",
        "correctAnswer": "volatile 关键字主要用于保证变量的可见性和禁止指令重排序...",
        "userAnswer": "volatile 可以保证线程安全...",
        "issues": ["理解不全面", "未提到禁止指令重排序"],
        "sourceReportId": 1,
        "reviewCount": 0,
        "lastReviewAt": null,
        "createdAt": "2026-03-19T10:00:00"
      }
    ]
  }
}
```

---

### 13.2 标记错题已复习

**接口路径**：`POST /api/v1/practice/mistakes/{id}/review`
**是否需要认证**：是

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | integer | 是 | 错题ID |

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| understood | boolean | 是 | 是否已理解 |
| notes | string | 否 | 复习笔记 |

**请求示例**：

```json
{
  "understood": true,
  "notes": "已理解 volatile 的可见性和禁止指令重排序两个作用"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "复习记录已保存",
  "data": {
    "reviewCount": 1,
    "lastReviewAt": "2026-03-20T10:00:00"
  }
}
```

---

### 13.3 删除错题

**接口路径**：`DELETE /api/v1/practice/mistakes/{id}`
**是否需要认证**：是

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | integer | 是 | 错题ID |

**响应示例**：

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

## 十四、帮助中心接口

### 14.1 获取帮助文章列表

**接口路径**：`GET /api/v1/help/articles`
**是否需要认证**：否

**查询参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| category | string | 否 | 分类：tutorial/tips/faq |
| page | integer | 否 | 页码，默认1 |
| pageSize | integer | 否 | 每页数量，默认20 |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 20,
    "page": 1,
    "pageSize": 20,
    "list": [
      {
        "id": 1,
        "category": "tutorial",
        "title": "如何开始你的第一次模拟面试",
        "summary": "本教程将指导你完成第一次模拟面试的全过程",
        "sortOrder": 1,
        "createdAt": "2026-03-01T10:00:00"
      }
    ]
  }
}
```

---

### 14.2 获取帮助文章详情

**接口路径**：`GET /api/v1/help/articles/{id}`
**是否需要认证**：否

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | integer | 是 | 文章ID |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "category": "tutorial",
    "title": "如何开始你的第一次模拟面试",
    "content": "# 如何开始你的第一次模拟面试\n\n## 1. 选择岗位\n\n首先，你需要选择一个面试岗位...",
    "sortOrder": 1,
    "createdAt": "2026-03-01T10:00:00"
  }
}
```

---

### 14.3 获取FAQ列表

**接口路径**：`GET /api/v1/help/faq`
**是否需要认证**：否

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "question": "面试过程中可以暂停吗？",
      "answer": "可以，面试过程中点击"暂停"按钮即可暂停计时。暂停期间不会影响评估结果。",
      "sortOrder": 1
    },
    {
      "id": 2,
      "question": "语音识别支持哪些格式？",
      "answer": "目前支持 WAV、MP3、WebM 格式的音频文件。",
      "sortOrder": 2
    }
  ]
}
```

---

## 十五、错误码说明

| 错误码 | 说明 |
| :--- | :--- |
| 10001 | 用户名已存在 |
| 10002 | 邮箱已存在 |
| 10003 | 用户名或密码错误 |
| 10004 | Token 无效 |
| 10005 | Token 已过期 |
| 20001 | 面试会话不存在 |
| 20002 | 面试会话状态错误 |
| 20003 | 面试时长已到 |
| 30001 | 评估报告不存在 |
| 30002 | 评估报告生成失败 |
| 40001 | 知识库文档不存在 |
| 40002 | 知识库检索失败 |
| 50001 | AI 服务异常 |
| 50002 | 语音识别失败 |
| 50003 | 语音合成失败 |
| 60001 | 岗位不存在 |
| 60002 | 岗位已禁用 |
| 70001 | 练习计划不存在 |
| 70002 | 错题记录不存在 |

---

> **提示**：完整的 OpenAPI 规范文档请参考 [apifox-collection.json](./apifox-collection.json)，可导入 Apifox 或 Swagger 进行在线测试。
