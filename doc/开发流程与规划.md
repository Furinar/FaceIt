# FaceIt 开发流程与规划

## 项目时间线

**项目周期**：约32天（2026年3月20日 - 2026年4月20日）
**截止日期**：2026年4月20日
**团队配置**：5人（1前端 + 2后端 + 1RAG知识库 + 1AI对接）
**部署方式**：不使用Docker，采用直接部署

---

## 一、团队成员与主要职责

| 成员 | 角色 | 主要技术栈 | 核心职责 |
| :--- | :--- | :--- | :--- |
| 成员 A | 前端工程师 | Next.js 14、React 18、TypeScript | 所有前端界面与交互 |
| 成员 B | 后端工程师（核心） | Spring Boot 3.x、PostgreSQL、Redis | 核心业务 API 与数据库 |
| 成员 C | 后端工程师（支持） | Spring Boot 3.x、PostgreSQL | 知识库管理、推荐系统、测试 |
| 成员 D | RAG 知识库构建 | Python、LangChain、Milvus | RAG 知识库构建与检索 |
| 成员 E | AI 对接与调教 | Python、大模型 API、Prompt | AI 调教与评估算法 |

---

## 二、详细开发计划

### 第1阶段：架构搭建（3月20日-3月24日，5天）

**本周目标**：完成项目架构搭建，验证核心技术可行性

#### 成员 A（前端）- 第1阶段任务

**工作量**：约 25 小时

**任务清单**：

- [ ] **Day 1**：初始化 Next.js 项目

  ```bash
  npx create-next-app@latest faceit-frontend --typescript --tailwind --app
  cd faceit-frontend
  npm install axios socket.io-client echarts
  ```

  - 配置 TypeScript、Tailwind CSS、ESLint
  - 配置环境变量（.env.local）
  - 设置代码规范（prettier、eslint）

- [ ] **Day 2-3**：搭建项目基础架构
  - 创建目录结构（app、components、lib、types）
  - 配置路由结构（App Router）
  - 创建基础 Layout 组件
  - 搭建通用组件库（Button、Input、Card、Modal）
  - 配置 Axios 实例与拦截器

- [ ] **Day 4**：设计页面原型
  - 绘制主要页面线框图（Figma / 手绘）
  - 确定 UI 风格与配色方案
  - 准备图标与素材资源

- [ ] **Day 5**：技术验证
  - 测试 MediaRecorder API（语音录制）
  - 测试 SSE 客户端接收（流式文本）
  - 测试 ECharts 基础图表

**交付物**：

- Next.js 项目骨架
- 基础组件库
- 页面原型设计稿

---

#### 成员 B（后端核心）- 第1阶段任务

**工作量**：约 30 小时

**任务清单**：

- [ ] **Day 1**：初始化 Spring Boot 项目

  ```bash
  # 使用 Spring Initializr 创建项目
  # 依赖：Spring Web, Spring Security, WebSocket, MyBatis-Plus,
  #      PostgreSQL Driver, Redis, Lombok, Validation
  ```

  - 配置 application.yml（数据库、Redis、端口）
  - 配置 PostgreSQL 连接（直接安装，不使用Docker）
  - 配置 Redis 连接（直接安装，不使用Docker）
  - 搭建项目分层架构（controller、service、mapper、entity、dto）

- [ ] **Day 2-3**：设计数据库表结构
  - 设计 14 张核心表（参考数据库设计文档）
  - 编写建表 SQL 脚本（schema.sql）
  - 注意 PostgreSQL 特有语法（SERIAL、JSONB、ARRAY）
  - 创建数据库索引
  - 编写测试数据脚本（test_data.sql）

- [ ] **Day 4**：配置 Spring Security + JWT
  - 实现 JWT 工具类（生成、验证）
  - 配置 Security Filter Chain
  - 实现认证入口点与访问拒绝处理器

- [ ] **Day 5**：搭建 Swagger API 文档
  - 集成 Knife4j（Swagger UI 增强版）
  - 配置 API 分组与描述
  - 编写示例接口测试

**交付物**：

- Spring Boot 项目骨架
- PostgreSQL 数据库建表脚本
- JWT 认证配置
- Swagger API 文档框架

---

#### 成员 C（后端支持）- 第1阶段任务

**工作量**：约 20 小时

**任务清单**：

- [ ] **Day 1-2**：熟悉项目架构与代码规范
  - 学习 Spring Boot 项目结构
  - 学习 PostgreSQL 数据库设计
  - 学习 MyBatis-Plus 使用

- [ ] **Day 3-4**：协助搭建项目基础架构
  - 协助编写数据库测试数据脚本
  - 配置单元测试环境（JUnit 5 + Mockito）

- [ ] **Day 5**：编写基础工具类
  - 编写通用响应类（Result）
  - 编写分页工具类
  - 编写日期时间工具类

**交付物**：

- 测试数据脚本
- 单元测试环境配置
- 通用工具类

---

#### 成员 D（RAG 知识库）- 第1阶段任务

**工作量**：约 35 小时

**任务清单**：

- [ ] **Day 1-2**：收集整理面试题库
  - **Java 后端题库**（目标：200+ 题）
    - Spring Boot 核心原理（30题）
    - MySQL 数据库优化（25题）
    - Redis 缓存应用（20题）
    - JVM 原理与调优（25题）
    - 多线程与并发（30题）
    - 设计模式（20题）
    - 分布式系统（25题）
    - 项目经历深挖（25题）
  - **Web 前端题库**（目标：200+ 题）
    - JavaScript 核心（30题）
    - Vue/React 框架（30题）
    - CSS 布局与动画（20题）
    - 浏览器原理（25题）
    - 性能优化（20题）
    - 工程化与构建（20题）
    - 前端安全（15题）
    - 项目经历深挖（25题）

- [ ] **Day 3**：整理技术栈文档与优秀回答
  - 编写技术栈清单（Markdown 格式）
  - 收集优秀回答范例（每个岗位 50+ 个）
  - 整理面试技巧文档（STAR 法则、沟通技巧）

- [ ] **Day 4**：搭建 Milvus 向量数据库

  ```bash
  # 直接安装 Milvus（不使用Docker）
  # 参考：https://milvus.io/docs/install_standalone-docker.md
  ```

  - 安装 Milvus Python SDK
  - 创建 Collection（定义 Schema）
  - 测试向量插入与检索

- [ ] **Day 5**：选择并测试 Embedding 模型
  - 测试 OpenAI text-embedding-3
  - 测试 BGE 中文 Embedding 模型
  - 对比效果并选择最优方案
  - 编写 Embedding 调用脚本

**交付物**：

- Java 后端题库（200+ 题，Markdown 格式）
- Web 前端题库（200+ 题，Markdown 格式）
- 技术栈文档与优秀回答范例
- Milvus 数据库环境
- Embedding 模型测试报告

---

#### 成员 E（AI 调教）- 第1阶段任务

**工作量**：约 30 小时

**任务清单**：

- [ ] **Day 1**：调研大语言模型 API
  - 测试智谱 GLM-4（https://open.bigmodel.cn/）
  - 测试文心一言（https://cloud.baidu.com/）
  - 测试讯飞星火（https://xinghuo.xfyun.cn/）
  - 对比：响应速度、输出质量、成本、API 限制
  - 选择最适合的模型

- [ ] **Day 2-3**：初始化 AI 推理服务项目

  ```bash
  mkdir faceit-ai-service
  cd faceit-ai-service
  python -m venv venv
  source venv/bin/activate
  pip install fastapi uvicorn zhipuai langchain sse-starlette
  ```

  - 搭建 FastAPI 项目结构
  - 配置环境变量（API Key）
  - 实现健康检查接口

- [ ] **Day 4**：设计初版 Prompt 模板
  - **Java 后端面试官 Prompt**
    ```
    你是一位资深的 Java 后端面试官，拥有 10 年以上的技术面试经验...
    ```
  - **Web 前端面试官 Prompt**
    ```
    你是一位资深的 Web 前端面试官，拥有 10 年以上的技术面试经验...
    ```
  - 设计不同面试阶段的 Prompt 变体

- [ ] **Day 5**：实现基础对话功能
  - 实现单轮对话接口
  - 测试 Prompt 效果
  - 调整 temperature、top_p 等参数

**交付物**：

- 大模型选型报告
- FastAPI 项目骨架
- 初版 Prompt 模板
- 基础对话接口

---

**第1阶段里程碑验收**：

- ✅ 前端项目可运行，基础组件库完成
- ✅ 后端项目可运行，数据库表创建完成
- ✅ 知识库题库收集完成（400+ 题）
- ✅ AI 服务可运行，基础对话功能完成

---

### 第2阶段：核心功能开发（3月25日-4月7日，14天）

**本周目标**：完成用户认证、面试会话管理、知识库向量化、AI对话服务

#### 第1周（3月25日-3月31日）：认证与会话管理

##### 成员 A（前端）任务

**工作量**：约 30 小时

- [ ] **Day 1-2**：实现用户认证页面
  - 登录页面（/login）
  - 注册页面（/register）
  - 表单验证（邮箱、密码强度）
  - 错误提示与加载状态
  - 对接登录/注册 API

- [ ] **Day 3-4**：实现 JWT Token 管理
  - 封装 Token 存储（localStorage）
  - 配置 Axios 拦截器（自动添加 Authorization header）
  - 实现 Token 过期处理（跳转登录页）
  - 实现登出功能

- [ ] **Day 5-6**：实现用户信息管理页面
  - 个人中心页面（/profile）
  - 用户信息展示与编辑
  - 头像上传（可选）
  - 密码修改功能

- [ ] **Day 7**：优化与测试
  - 路由守卫（未登录跳转到登录页）
  - 表单验证优化
  - 错误处理优化
  - 响应式适配

**交付物**：

- 用户认证完整流程
- 用户信息管理页面

---

##### 成员 B（后端核心）任务

**工作量**：约 35 小时

- [ ] **Day 1-2**：实现用户认证 API

  ```java
  // UserController.java
  @PostMapping("/auth/register")
  public Result register(@RequestBody @Valid RegisterDTO dto)

  @PostMapping("/auth/login")
  public Result<LoginVO> login(@RequestBody @Valid LoginDTO dto)

  @PostMapping("/auth/logout")
  public Result logout()
  ```

  - 实现用户注册（密码 BCrypt 加密）
  - 实现用户登录（生成 JWT Token，有效期24小时）
  - 实现登出（Token 加入 Redis 黑名单）

- [ ] **Day 3-4**：实现用户管理 API

  ```java
  @GetMapping("/user/profile")
  public Result<UserVO> getProfile()

  @PutMapping("/user/profile")
  public Result updateProfile(@RequestBody @Valid UpdateProfileDTO dto)

  @PutMapping("/user/password")
  public Result updatePassword(@RequestBody @Valid UpdatePasswordDTO dto)
  ```

- [ ] **Day 5-6**：实现面试会话管理 API

  ```java
  @PostMapping("/interview/create")
  public Result<InterviewSessionVO> create(@RequestBody @Valid CreateInterviewDTO dto)

  @PostMapping("/interview/{id}/start")
  public Result start(@PathVariable Long id)

  @PostMapping("/interview/{id}/end")
  public Result end(@PathVariable Long id)

  @GetMapping("/interview/{id}")
  public Result<InterviewSessionVO> getDetail(@PathVariable Long id)

  @GetMapping("/interview/history")
  public Result<PageVO<InterviewSessionVO>> getHistory(@RequestParam int page, @RequestParam int pageSize)
  ```

- [ ] **Day 7**：编写单元测试
  - 用户认证测试
  - 面试会话测试
  - 使用 Apifox 测试所有接口

**交付物**：

- 用户认证完整 API
- 面试会话管理 API
- 单元测试代码

---

##### 成员 C（后端支持）任务

**工作量**：约 25 小时

- [ ] **Day 1-3**：实现知识库管理 API
  - 获取知识库列表
  - 获取知识库文档详情
  - 知识库搜索功能

- [ ] **Day 4-5**：编写用户认证模块单元测试
  - 注册功能测试
  - 登录功能测试
  - Token 验证测试

- [ ] **Day 6-7**：编写面试会话模块单元测试
  - 创建会话测试
  - 会话状态管理测试

**交付物**：

- 知识库管理 API
- 单元测试代码

---

##### 成员 D（RAG 知识库）任务

**工作量**：约 35 小时

- [ ] **Day 1-2**：实现文档切片算法

  ```python
  # document_splitter.py
  def split_document(content: str, chunk_size: int = 500) -> List[Chunk]:
      """
      将文档切分为固定大小的片段
      - 按段落切分
      - 保留上下文信息
      - 处理代码块
      """
      pass
  ```

  - 实现按段落切分
  - 实现重叠窗口（overlap）
  - 处理 Markdown 格式
  - 处理代码块

- [ ] **Day 3-4**：实现文档向量化

  ```python
  # embedding_service.py
  def embed_texts(texts: List[str]) -> List[List[float]]:
      """
      批量生成文本向量
      - 调用 Embedding API
      - 批量处理优化
      - 错误重试机制
      """
      pass
  ```

  - 对接 Embedding API（OpenAI / BGE）
  - 实现批量处理（每批 100 条）
  - 实现进度显示
  - 实现错误重试

- [ ] **Day 5-6**：导入知识库到 Milvus

  ```python
  # knowledge_importer.py
  def import_knowledge_base(job_position: str):
      """
      导入知识库到 Milvus
      1. 读取 Markdown 文件
      2. 切片
      3. 向量化
      4. 存入 Milvus
      5. 元数据存入 PostgreSQL
      """
      pass
  ```

  - 批量导入 Java 后端题库
  - 批量导入 Web 前端题库
  - 存储元数据到 PostgreSQL
  - 验证导入结果

- [ ] **Day 7**：测试检索效果
  - 编写测试查询
  - 测试检索准确率
  - 调整切片大小
  - 调整检索参数

**交付物**：

- 文档切片与向量化代码
- 知识库导入脚本
- Milvus 数据库（已导入数据）
- 检索效果测试报告

---

##### 成员 E（AI 调教）任务

**工作量**：约 35 小时

- [ ] **Day 1-2**：完善 Prompt 模板

  ```python
  # prompts.py
  JAVA_BACKEND_INTERVIEWER = """
  你是一位资深的 Java 后端面试官，拥有 10 年以上的技术面试经验。

  面试要求：
  1. 根据候选人的回答进行深入追问，挖掘技术细节
  2. 关注候选人的逻辑思维和表达能力
  3. 模拟真实面试场景，保持专业且友好的态度
  4. 每次提问前，先从知识库检索相关内容作为参考

  当前面试阶段：{stage}
  候选人背景：{user_background}

  请根据以下检索到的知识库内容提问：
  {rag_context}
  """
  ```

  - 完善 Java 后端面试官 Prompt
  - 完善 Web 前端面试官 Prompt
  - 设计不同阶段的 Prompt 变体
  - 添加 Few-shot 示例

- [ ] **Day 3-4**：实现多轮对话管理

  ```python
  # conversation_manager.py
  class ConversationManager:
      def __init__(self, session_id: str):
          self.session_id = session_id
          self.history = []  # 对话历史
          self.stage = "self_intro"  # 当前阶段

      def add_message(self, role: str, content: str):
          """添加消息到历史"""
          pass

      def get_context(self, max_turns: int = 10) -> str:
          """获取最近 N 轮对话作为上下文"""
          pass

      def update_stage(self):
          """根据对话轮数更新面试阶段"""
          pass
  ```

  - 实现对话历史管理
  - 实现上下文压缩（保留最近 10 轮）
  - 实现面试阶段状态机
  - 实现对话历史持久化（Redis）

- [ ] **Day 5-6**：实现流式响应

  ```python
  # ai_service.py
  @app.post("/ai/chat")
  async def chat(request: ChatRequest):
      """
      流式对话接口
      - 接收用户消息
      - 调用 RAG 检索
      - 调用大模型生成回复
      - 流式返回（SSE）
      """
      async def generate():
          # 调用大模型 API（流式）
          for chunk in llm.stream(prompt):
              yield f"data: {json.dumps({'content': chunk})}\n\n"

      return StreamingResponse(generate(), media_type="text/event-stream")
  ```

  - 实现 SSE 流式响应
  - 对接 RAG 检索服务
  - 实现动态 Prompt 构建
  - 测试流式效果

- [ ] **Day 7**：优化对话质量
  - 测试不同 temperature 参数
  - 测试不同 Prompt 策略
  - 收集测试反馈
  - 迭代优化

**交付物**：

- 完善的 Prompt 模板库
- 多轮对话管理代码
- 流式对话接口
- 对话质量测试报告

---

**第1周里程碑验收**：

- ✅ 用户可以注册、登录、管理个人信息
- ✅ 用户可以创建面试会话
- ✅ 知识库已导入 Milvus（400+ 题）
- ✅ AI 可以进行多轮对话（流式响应）

---

#### 第2周（4月1日-4月7日）：面试室与多模态

##### 成员 A（前端）任务

**工作量**：约 35 小时

- [ ] **Day 1-2**：实现岗位选择与面试配置页面
  - 岗位选择（Java 后端 / Web 前端）
  - 面试模式选择（基础 / 全真 / 专项）
  - 面试时长设置（10 / 20 / 30 分钟）
  - 显示岗位技术栈与考点
  - 对接创建面试会话 API

- [ ] **Day 3-5**：开发 AI 模拟面试室核心界面

  ```tsx
  // app/interview/[id]/page.tsx
  export default function InterviewRoom({
    params,
  }: {
    params: { id: string };
  }) {
    const [messages, setMessages] = useState<Message[]>([]);
    const [isRecording, setIsRecording] = useState(false);
    const [timeLeft, setTimeLeft] = useState(1800); // 30分钟

    // WebSocket 连接
    useEffect(() => {
      const ws = new WebSocket(`ws://localhost:8080/ws/interview/${params.id}`);
      // ...
    }, []);

    // 语音录制
    const startRecording = async () => {
      const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
      const mediaRecorder = new MediaRecorder(stream);
      // ...
    };

    return (
      <div className="interview-room">
        {/* 对话区域 */}
        <div className="messages">
          {messages.map((msg) => (
            <MessageBubble key={msg.id} message={msg} />
          ))}
        </div>

        {/* 输入区域 */}
        <div className="input-area">
          <button onClick={startRecording}>🎤 语音输入</button>
          <input type="text" placeholder="或输入文字..." />
          <button>发送</button>
        </div>

        {/* 倒计时 */}
        <div className="timer">{formatTime(timeLeft)}</div>
      </div>
    );
  }
  ```

  - 实时对话气泡展示
  - 语音录制按钮与状态指示
  - 文字输入框与发送功能
  - 倒计时与进度条
  - 流式文本展示（SSE）

- [ ] **Day 6**：实现 WebRTC 音频录制
  - 获取麦克风权限
  - 录制音频（WebM / MP3）
  - 实时音量显示
  - 上传音频到后端

- [ ] **Day 7**：实现 SSE 流式接收
  - 连接 SSE 端点
  - 接收流式文本
  - 逐字展示（打字机效果）
  - 错误处理与重连

**交付物**：

- 岗位选择与配置页面
- AI 模拟面试室完整界面
- 语音录制功能
- 流式文本展示

---

##### 成员 B（后端核心）任务

**工作量**：约 35 小时

- [ ] **Day 1-2**：实现 WebSocket 服务

  ```java
  // WebSocketConfig.java
  @Configuration
  @EnableWebSocket
  public class WebSocketConfig implements WebSocketConfigurer {
      @Override
      public void registerWebSocketHandlers(WebSocketHandlerRegistry registry) {
          registry.addHandler(interviewWebSocketHandler(), "/ws/interview/{sessionId}")
                  .setAllowedOrigins("*");
      }
  }

  // InterviewWebSocketHandler.java
  @Component
  public class InterviewWebSocketHandler extends TextWebSocketHandler {
      @Override
      protected void handleTextMessage(WebSocketSession session, TextMessage message) {
          // 处理用户消息
          // 调用 AI 服务
          // 推送 AI 回复
      }
  }
  ```

- [ ] **Day 3-4**：实现对话记录存储

  ```java
  @PostMapping("/interview/{id}/message")
  public Result sendMessage(@PathVariable Long id, @RequestBody SendMessageDTO dto) {
      // 1. 保存用户消息到数据库
      // 2. 调用 AI 服务获取回复
      // 3. 保存 AI 回复到数据库
      // 4. 返回结果
  }

  @GetMapping("/interview/{id}/messages")
  public Result<PageVO<MessageVO>> getMessages(@PathVariable Long id, @RequestParam int page) {
      // 分页查询对话历史
  }
  ```

- [ ] **Day 5-6**：集成多模态服务

  ```java
  // AudioService.java
  @Service
  public class AudioService {
      public String asr(MultipartFile audioFile) {
          // 调用讯飞 ASR API
          // 返回识别文本
      }

      public byte[] tts(String text) {
          // 调用讯飞 TTS API
          // 返回音频数据
      }
  }

  @PostMapping("/audio/asr")
  public Result<AsrVO> asr(@RequestParam("file") MultipartFile file) {
      String text = audioService.asr(file);
      return Result.success(new AsrVO(text));
  }
  ```

- [ ] **Day 7**：对接 AI 推理服务
  - 调用成员 E 的 AI 服务接口
  - 处理流式响应
  - 错误处理与重试

**交付物**：

- WebSocket 实时通信服务
- 对话记录存储
- 多模态服务集成
- AI 服务对接

---

##### 成员 C（后端支持）任务

**工作量**：约 25 小时

- [ ] **Day 1-3**：实现知识库切片管理
  - 对接 RAG 服务
  - 实现知识库文档上传
  - 实现知识库文档删除

- [ ] **Day 4-5**：编写知识库模块单元测试
  - 知识库列表查询测试
  - 知识库搜索测试

- [ ] **Day 6-7**：编写集成测试
  - 用户认证集成测试
  - 面试会话集成测试

**交付物**：

- 知识库切片管理代码
- 单元测试代码
- 集成测试代码

---

##### 成员 D（RAG 知识库）任务

**工作量**：约 35 小时

- [ ] **Day 1-3**：实现 RAG 检索服务

  ```python
  # rag_service.py
  from fastapi import FastAPI
  from langchain.vectorstores import Milvus
  from langchain.embeddings import OpenAIEmbeddings

  app = FastAPI()

  @app.post("/knowledge/search")
  async def search(request: SearchRequest):
      """
      RAG 检索接口
      1. 向量检索（Milvus）
      2. 关键词检索（BM25）
      3. 混合检索（RRF 重排序）
      4. 返回 Top-K 结果
      """
      # 向量检索
      vector_results = milvus.similarity_search(
          request.query,
          k=10,
          filter={"job_position": request.job_position}
      )

      # 关键词检索
      keyword_results = bm25_search(request.query, k=10)

      # 混合重排序
      final_results = rrf_rerank(vector_results, keyword_results, k=5)

      return {"results": final_results}
  ```

- [ ] **Day 4-5**：实现知识库管理 API

  ```python
  @app.get("/knowledge/list")
  async def list_documents(job_position: str, category: str, page: int = 1):
      """获取知识库文档列表"""
      pass

  @app.get("/knowledge/{document_id}")
  async def get_document(document_id: int):
      """获取知识库文档详情"""
      pass

  @app.post("/knowledge/upload")
  async def upload_document(file: UploadFile, job_position: str, category: str):
      """上传知识库文档"""
      pass
  ```

- [ ] **Day 6**：优化检索准确率
  - 调整切片大小（300 / 500 / 700 字）
  - 调整 Top-K 参数（3 / 5 / 10）
  - 调整相似度阈值（0.7 / 0.8 / 0.9）
  - A/B 测试不同策略
  - 记录测试结果

- [ ] **Day 7**：性能优化
  - 添加 Redis 缓存（缓存热门查询）
  - 优化 Milvus 索引参数
  - 批量查询优化

**交付物**：

- RAG 检索服务（FastAPI）
- 知识库管理 API
- 检索准确率优化报告
- 性能优化报告

---

##### 成员 E（AI 调教）任务

**工作量**：约 35 小时

- [ ] **Day 1-3**：实现动态追问逻辑

  ```python
  # follow_up_generator.py
  class FollowUpGenerator:
      def generate_follow_up(self, user_answer: str, rag_context: str) -> str:
          """
          生成追问
          1. 提取关键词
          2. 调用 RAG 检索
          3. 构建追问 Prompt
          4. 调用大模型生成
          """
          # 提取技术关键词
          keywords = extract_keywords(user_answer)

          # RAG 检索相关题目
          rag_results = rag_service.search(keywords)

          # 构建追问 Prompt
          prompt = f"""
          候选人刚才回答：{user_answer}

          请根据以下知识库内容进行深入追问：
          {rag_results}

          追问要求：
          1. 针对回答中的技术点深挖
          2. 避免重复已问过的问题
          3. 保持追问的自然性
          """

          # 调用大模型
          follow_up = llm.generate(prompt)
          return follow_up
  ```

  - 实现关键词提取（jieba / spaCy）
  - 实现追问深度控制（最多追问 3 次）
  - 实现追问多样性（避免重复）
  - 测试追问效果

- [ ] **Day 4-5**：优化面试官对话质量
  - 收集 20+ 个测试案例
  - 测试不同 Prompt 策略
  - 调整 temperature（0.7 / 0.8 / 0.9）
  - 调整 top_p（0.9 / 0.95 / 1.0）
  - 记录测试结果并迭代

- [ ] **Day 6-7**：处理边界情况
  ```python
  # edge_case_handler.py
  def handle_edge_cases(user_answer: str) -> str:
      """处理边界情况"""
      # 回答过短（< 10 字）
      if len(user_answer) < 10:
          return "你的回答比较简短，能否详细说明一下？"

      # 答非所问
      if not is_relevant(user_answer, current_question):
          return "你的回答似乎偏离了问题，我们再回到刚才的问题..."

      # 用户沉默（超时）
      if is_timeout():
          return "看起来你需要一些时间思考，我们可以换一个问题..."

      return None
  ```

**交付物**：

- 动态追问逻辑代码
- 对话质量优化报告
- 边界情况处理代码

---

**第2周里程碑验收**：

- ✅ 用户可以进入面试室进行完整的语音/文字面试
- ✅ AI 可以根据回答动态追问
- ✅ RAG 检索服务正常工作
- ✅ 流式对话体验流畅

---

### 第3阶段：评估与报告（4月8日-4月12日，5天）

**本周目标**：完成评估报告生成、成长曲线、推荐系统

#### 成员 A（前端）- 第3阶段任务

**工作量**：约 30 小时

**任务清单**：

- [ ] **Day 1-2**：实现评估报告展示页面
  - 报告概览（综合得分、评级）
  - 多维度得分展示
  - 逐题点评列表
  - 改进建议展示

- [ ] **Day 3**：集成 ECharts 绘制雷达图
  - 能力雷达图
  - 历史对比雷达图
  - 交互式图表

- [ ] **Day 4**：实现成长曲线可视化
  - 综合分数趋势图
  - 各维度能力趋势图
  - 时间轴展示

- [ ] **Day 5**：实现知识库浏览页面
  - 分岗位展示知识库
  - 搜索功能
  - 文档详情展示

**交付物**：

- 评估报告展示页面
- 雷达图可视化
- 成长曲线可视化
- 知识库浏览页面

---

#### 成员 B（后端核心）- 第3阶段任务

**工作量**：约 35 小时

**任务清单**：

- [ ] **Day 1-2**：实现评估报告生成 API

  ```java
  @PostMapping("/evaluation/generate")
  public Result generateEvaluation(@RequestBody GenerateEvaluationDTO dto) {
      // 1. 获取完整对话历史
      // 2. 调用 AI 评估服务
      // 3. 生成多维度评分
      // 4. 生成逐题点评
      // 5. 生成改进建议
      // 6. 存储评估报告
      // 7. 更新能力成长记录
      // 8. 生成推荐练习
      return Result.success(report);
  }
  ```

- [ ] **Day 3**：实现评估报告查询 API

  ```java
  @GetMapping("/evaluation/{id}")
  public Result<EvaluationReportVO> getEvaluation(@PathVariable Long id)

  @GetMapping("/evaluation/{id}/questions")
  public Result<List<QuestionEvaluationVO>> getQuestionEvaluations(@PathVariable Long id)

  @GetMapping("/evaluation/history")
  public Result<PageVO<EvaluationReportVO>> getEvaluationHistory(@RequestParam int page)
  ```

- [ ] **Day 4**：实现统计分析 API

  ```java
  @GetMapping("/stats/growth-curve")
  public Result<GrowthCurveVO> getGrowthCurve(@RequestParam String jobPosition)

  @GetMapping("/stats/radar-chart")
  public Result<RadarChartVO> getRadarChart(@RequestParam String jobPosition)

  @GetMapping("/stats/weak-points")
  public Result<WeakPointsVO> getWeakPoints(@RequestParam String jobPosition)
  ```

- [ ] **Day 5**：实现推荐练习 API

  ```java
  @GetMapping("/stats/recommendations")
  public Result<List<RecommendationVO>> getRecommendations()

  @PutMapping("/stats/recommendations/{id}")
  public Result updateRecommendationStatus(@PathVariable Long id, @RequestBody UpdateStatusDTO dto)
  ```

**交付物**：

- 评估报告生成 API
- 评估报告查询 API
- 统计分析 API
- 推荐练习 API

---

#### 成员 C（后端支持）- 第3阶段任务

**工作量**：约 30 小时

**任务清单**：

- [ ] **Day 1-2**：实现推荐系统
  - 基于评估结果的推荐
  - 薄弱知识点推荐
  - 练习计划生成

- [ ] **Day 3-4**：实现统计分析服务
  - 用户面试统计
  - 知识库使用统计
  - 系统整体统计

- [ ] **Day 5**：编写评估模块单元测试
  - 评估报告生成测试
  - 统计分析测试
  - 推荐系统测试

**交付物**：

- 推荐系统代码
- 统计分析服务代码
- 单元测试代码

---

#### 成员 D（RAG 知识库）- 第3阶段任务

**工作量**：约 25 小时

**任务清单**：

- [ ] **Day 1-2**：实现知识库增量更新
  - 新增文档自动切片
  - 新增文档自动向量化
  - 增量更新 Milvus

- [ ] **Day 3**：实现知识库版本管理
  - 文档版本记录
  - 变更历史追踪

- [ ] **Day 4**：优化检索速度
  - Redis 缓存热门查询
  - Milvus 索引优化
  - 批量查询优化

- [ ] **Day 5**：编写知识库使用文档

**交付物**：

- 知识库增量更新代码
- 知识库版本管理代码
- 性能优化报告
- 知识库使用文档

---

#### 成员 E（AI 调教）- 第3阶段任务

**工作量**：约 35 小时

**任务清单**：

- [ ] **Day 1-2**：实现面试评估算法

  ```python
  # evaluation_engine.py
  class EvaluationEngine:
      def evaluate_interview(self, conversation_history: List[Message]) -> EvaluationReport:
          """
          评估面试表现
          1. 技术正确性评分
          2. 逻辑严谨性评分
          3. 岗位匹配度评分
          4. 表达能力评分
          5. 自信度评分
          6. 生成逐题点评
          7. 生成改进建议
          """
          pass
  ```

- [ ] **Day 3**：实现逐题点评生成
  - 分析每道题的回答质量
  - 提取亮点与问题
  - 生成改进方向

- [ ] **Day 4-5**：实现改进建议生成
  - 基于薄弱环节生成建议
  - 推荐学习资源
  - 生成练习计划

**交付物**：

- 面试评估算法代码
- 逐题点评生成代码
- 改进建议生成代码

---

**第3阶段里程碑验收**：

- ✅ 面试结束后可以生成完整评估报告
- ✅ 用户可以查看成长曲线
- ✅ 系统可以推荐练习内容
- ✅ 评估结果准确、建议有价值

---

### 第4阶段：系统联调（4月13日-4月17日，5天）

**本周目标**：完成系统联调、性能优化、Bug 修复

#### 全员协作任务

- [ ] **Day 1-2**：全流程测试
  - 用户注册登录流程
  - 面试创建与进行流程
  - 评估报告生成流程
  - 成长曲线查看流程
  - 知识库浏览流程

- [ ] **Day 3-4**：性能优化
  - 前端性能优化（代码分割、懒加载）
  - 后端性能优化（数据库查询、缓存）
  - AI 服务优化（Token 消耗、响应速度）
  - RAG 检索优化（缓存、索引）

- [ ] **Day 5**：Bug 修复与压力测试
  - 收集测试中发现的问题
  - 按优先级排序
  - 逐个修复并验证
  - 模拟多用户并发访问
  - 测试系统稳定性

---

### 第5阶段：文档与提交（4月18日-4月20日，3天）

**本周目标**：完善文档、录制视频、准备提交材料

#### 全员协作任务

- [ ] **Day 1**：完善项目文档
  - 项目概要介绍
  - 项目详细方案
  - API 接口文档
  - 部署文档（直接部署方式，不使用Docker）

- [ ] **Day 2**：录制演示视频
  - 编写演示脚本
  - 录制核心功能演示
  - 视频剪辑与后期
  - 准备提交材料

- [ ] **Day 3**：最终检查与提交
  - 检查所有提交材料
  - 确认材料完整性
  - 提交参赛材料

---

## 三、关键里程碑

| 时间 | 里程碑 | 验收标准 |
| :--- | :--- | :--- |
| 3月24日 | 架构搭建完成 | 前后端项目可运行，数据库表创建完成 |
| 3月31日 | 用户认证完成 | 可以注册、登录、获取用户信息 |
| 4月7日 | 面试功能完成 | 可以进行完整的语音/文字面试对话 |
| 4月12日 | 评估功能完成 | 可以生成多维度评估报告 |
| 4月17日 | 系统联调完成 | 全流程测试通过，无重大 Bug |
| 4月20日 | 提交材料完成 | 所有交付物准备就绪并提交 |

---

## 四、风险应对

| 风险 | 应对措施 | 负责人 |
| :--- | :--- | :--- |
| 大模型 API 成本超预算 | 使用免费额度 + 优化 Token 消耗 | 成员 E |
| RAG 检索效果不佳 | 优化切片策略 + 增加知识库质量 | 成员 D |
| 前后端接口对接问题 | 使用 Apifox Mock 数据 + 及时沟通 | 成员 A、B |
| 开发进度延期 | 每周复盘调整 + 优先级排序 | 全员 |
| 技术难点攻克困难 | 结对编程 + 技术分享会 | 全员 |

---

> **提示**：本开发规划可根据实际进度动态调整。关键是保持高效沟通，确保各模块按时交付。
