# FaceIt 数据库设计

## 一、数据库选型

**主数据库**：PostgreSQL 15+
**缓存数据库**：Redis 7.0
**向量数据库**：Milvus 2.3 / ChromaDB

---

## 二、数据库表设计

### 2.1 用户表 (users)

存储用户基本信息与账号数据。

| 字段名 | 类型 | 长度 | 约束 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGSERIAL | - | PRIMARY KEY | 用户ID |
| username | VARCHAR | 50 | UNIQUE, NOT NULL | 用户名 |
| password | VARCHAR | 255 | NOT NULL | 密码（BCrypt加密） |
| email | VARCHAR | 100 | UNIQUE | 邮箱 |
| phone | VARCHAR | 20 | | 手机号 |
| real_name | VARCHAR | 50 | | 真实姓名 |
| school | VARCHAR | 100 | | 学校 |
| major | VARCHAR | 100 | | 专业 |
| grade | VARCHAR | 20 | | 年级 |
| target_position | VARCHAR | 50 | | 意向岗位 |
| job_status | VARCHAR | 20 | DEFAULT 'preparing' | 求职阶段：preparing/interviewing/offered |
| avatar_url | VARCHAR | 255 | | 头像URL |
| status | SMALLINT | - | DEFAULT 1 | 账号状态：0-禁用，1-正常 |
| created_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- PRIMARY KEY (id)
- UNIQUE KEY (username)
- UNIQUE KEY (email)
- INDEX (target_position)

---

### 2.2 面试会话表 (interview_sessions)

存储面试会话的基本信息。

| 字段名 | 类型 | 长度 | 约束 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGSERIAL | - | PRIMARY KEY | 会话ID |
| user_id | BIGINT | - | NOT NULL, FOREIGN KEY | 用户ID |
| job_position | VARCHAR | 50 | NOT NULL | 面试岗位：java_backend/web_frontend |
| interview_mode | VARCHAR | 20 | NOT NULL | 面试模式：basic/full/special |
| duration_minutes | INTEGER | - | NOT NULL | 面试时长（分钟） |
| status | VARCHAR | 20 | NOT NULL | 会话状态：pending/in_progress/completed/interrupted |
| start_time | TIMESTAMP | - | | 开始时间 |
| end_time | TIMESTAMP | - | | 结束时间 |
| actual_duration | INTEGER | - | | 实际时长（秒） |
| total_questions | INTEGER | - | DEFAULT 0 | 总提问数 |
| total_answers | INTEGER | - | DEFAULT 0 | 总回答数 |
| created_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- PRIMARY KEY (id)
- INDEX (user_id, created_at)
- INDEX (job_position)
- INDEX (status)

---

### 2.3 对话记录表 (conversation_messages)

存储面试过程中的每一轮对话。

| 字段名 | 类型 | 长度 | 约束 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGSERIAL | - | PRIMARY KEY | 消息ID |
| session_id | BIGINT | - | NOT NULL, FOREIGN KEY | 会话ID |
| role | VARCHAR | 20 | NOT NULL | 角色：interviewer/candidate |
| message_type | VARCHAR | 20 | NOT NULL | 消息类型：text/audio |
| content | TEXT | - | NOT NULL | 消息内容（文本） |
| audio_url | VARCHAR | 255 | | 音频文件URL |
| audio_duration | INTEGER | - | | 音频时长（秒） |
| stage | VARCHAR | 50 | | 面试阶段：self_intro/tech/project/scenario/behavior |
| sequence | INTEGER | - | NOT NULL | 消息序号 |
| created_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**：
- PRIMARY KEY (id)
- INDEX (session_id, sequence)
- INDEX (role)

---

### 2.4 评估报告表 (evaluation_reports)

存储面试评估报告的主表。

| 字段名 | 类型 | 长度 | 约束 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGSERIAL | - | PRIMARY KEY | 报告ID |
| session_id | BIGINT | - | UNIQUE, NOT NULL, FOREIGN KEY | 会话ID |
| user_id | BIGINT | - | NOT NULL, FOREIGN KEY | 用户ID |
| overall_score | DECIMAL | 5,2 | NOT NULL | 综合得分（0-100） |
| grade | VARCHAR | 5 | NOT NULL | 评级：S/A/B/C/D |
| technical_accuracy | DECIMAL | 5,2 | | 技术正确性得分 |
| logical_rigor | DECIMAL | 5,2 | | 逻辑严谨性得分 |
| job_matching | DECIMAL | 5,2 | | 岗位匹配度得分 |
| expression_ability | DECIMAL | 5,2 | | 表达能力得分 |
| confidence | DECIMAL | 5,2 | | 自信度得分 |
| strengths | JSONB | - | | 优势亮点（JSON数组） |
| weaknesses | JSONB | - | | 薄弱环节（JSON数组） |
| summary | TEXT | - | | 总结分析 |
| suggestions | JSONB | - | | 改进建议（JSON数组） |
| radar_chart_data | JSONB | - | | 雷达图数据（JSON） |
| created_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**：
- PRIMARY KEY (id)
- UNIQUE KEY (session_id)
- INDEX (user_id, created_at)
- INDEX (overall_score)

---

### 2.5 逐题评价表 (question_evaluations)

存储每道题的详细评价。

| 字段名 | 类型 | 长度 | 约束 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGSERIAL | - | PRIMARY KEY | 评价ID |
| report_id | BIGINT | - | NOT NULL, FOREIGN KEY | 报告ID |
| question_message_id | BIGINT | - | NOT NULL, FOREIGN KEY | 问题消息ID |
| answer_message_id | BIGINT | - | NOT NULL, FOREIGN KEY | 回答消息ID |
| question_content | TEXT | - | NOT NULL | 问题内容 |
| answer_content | TEXT | - | NOT NULL | 回答内容 |
| score | DECIMAL | 5,2 | | 单题得分（0-100） |
| correctness | VARCHAR | 20 | | 正确性：correct/partial/incorrect |
| highlights | JSONB | - | | 亮点（JSON数组） |
| issues | JSONB | - | | 问题点（JSON数组） |
| improvement | TEXT | - | | 改进方向 |
| created_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**：
- PRIMARY KEY (id)
- INDEX (report_id)
- INDEX (question_message_id)

---

### 2.6 知识库文档表 (knowledge_documents)

存储知识库文档的元数据。

| 字段名 | 类型 | 长度 | 约束 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGSERIAL | - | PRIMARY KEY | 文档ID |
| job_position | VARCHAR | 50 | NOT NULL | 所属岗位 |
| category | VARCHAR | 50 | NOT NULL | 分类：tech_stack/question/answer |
| title | VARCHAR | 200 | NOT NULL | 文档标题 |
| content | TEXT | - | NOT NULL | 文档内容 |
| file_path | VARCHAR | 255 | | 文件路径 |
| tags | VARCHAR | 255 | | 标签（逗号分隔） |
| difficulty | VARCHAR | 20 | | 难度：basic/intermediate/advanced |
| status | SMALLINT | - | DEFAULT 1 | 状态：0-禁用，1-启用 |
| created_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- PRIMARY KEY (id)
- INDEX (job_position, category)
- INDEX (difficulty)

---

### 2.7 知识库切片表 (knowledge_chunks)

存储知识库文档的切片，用于 RAG 检索。

| 字段名 | 类型 | 长度 | 约束 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGSERIAL | - | PRIMARY KEY | 切片ID |
| document_id | BIGINT | - | NOT NULL, FOREIGN KEY | 文档ID |
| chunk_index | INTEGER | - | NOT NULL | 切片序号 |
| content | TEXT | - | NOT NULL | 切片内容 |
| vector_id | VARCHAR | 100 | | 向量数据库中的ID |
| token_count | INTEGER | - | | Token数量 |
| created_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**：
- PRIMARY KEY (id)
- INDEX (document_id, chunk_index)
- UNIQUE KEY (vector_id)

---

### 2.8 能力成长记录表 (ability_growth_records)

存储用户能力成长数据，用于生成成长曲线。

| 字段名 | 类型 | 长度 | 约束 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGSERIAL | - | PRIMARY KEY | 记录ID |
| user_id | BIGINT | - | NOT NULL, FOREIGN KEY | 用户ID |
| report_id | BIGINT | - | NOT NULL, FOREIGN KEY | 报告ID |
| job_position | VARCHAR | 50 | NOT NULL | 岗位 |
| overall_score | DECIMAL | 5,2 | NOT NULL | 综合得分 |
| technical_accuracy | DECIMAL | 5,2 | | 技术正确性 |
| logical_rigor | DECIMAL | 5,2 | | 逻辑严谨性 |
| job_matching | DECIMAL | 5,2 | | 岗位匹配度 |
| expression_ability | DECIMAL | 5,2 | | 表达能力 |
| confidence | DECIMAL | 5,2 | | 自信度 |
| interview_count | INTEGER | - | NOT NULL | 累计面试次数 |
| created_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**：
- PRIMARY KEY (id)
- INDEX (user_id, created_at)
- INDEX (job_position)

---

### 2.9 推荐练习表 (practice_recommendations)

存储系统为用户推荐的练习内容。

| 字段名 | 类型 | 长度 | 约束 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGSERIAL | - | PRIMARY KEY | 推荐ID |
| user_id | BIGINT | - | NOT NULL, FOREIGN KEY | 用户ID |
| report_id | BIGINT | - | NOT NULL, FOREIGN KEY | 报告ID |
| recommendation_type | VARCHAR | 50 | NOT NULL | 推荐类型：knowledge/question/skill |
| title | VARCHAR | 200 | NOT NULL | 推荐标题 |
| description | TEXT | - | | 推荐描述 |
| resource_url | VARCHAR | 255 | | 资源链接 |
| priority | INTEGER | - | DEFAULT 0 | 优先级（数字越大越优先） |
| status | VARCHAR | 20 | DEFAULT 'pending' | 状态：pending/completed/ignored |
| created_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| completed_at | TIMESTAMP | - | | 完成时间 |

**索引**：
- PRIMARY KEY (id)
- INDEX (user_id, status)
- INDEX (report_id)

---

### 2.10 系统配置表 (system_configs)

存储系统配置参数。

| 字段名 | 类型 | 长度 | 约束 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGSERIAL | - | PRIMARY KEY | 配置ID |
| config_key | VARCHAR | 100 | UNIQUE, NOT NULL | 配置键 |
| config_value | TEXT | - | NOT NULL | 配置值 |
| description | VARCHAR | 255 | | 配置描述 |
| created_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- PRIMARY KEY (id)
- UNIQUE KEY (config_key)

---

### 2.11 岗位配置表 (job_positions)

存储面试岗位的配置信息。

| 字段名 | 类型 | 长度 | 约束 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGSERIAL | - | PRIMARY KEY | 岗位ID |
| code | VARCHAR | 50 | UNIQUE, NOT NULL | 岗位代码 |
| name | VARCHAR | 100 | NOT NULL | 岗位名称 |
| description | TEXT | - | | 岗位描述 |
| icon | VARCHAR | 50 | | 图标标识 |
| tech_stack | JSONB | - | | 技术栈清单（JSON数组） |
| exam_points | JSONB | - | | 面试考点（JSON数组） |
| dimensions | JSONB | - | | 评估维度配置（JSON数组） |
| interview_modes | JSONB | - | | 面试模式配置（JSON数组） |
| status | SMALLINT | - | DEFAULT 1 | 状态：0-禁用，1-启用 |
| sort_order | INTEGER | - | DEFAULT 0 | 排序序号 |
| created_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- PRIMARY KEY (id)
- UNIQUE KEY (code)
- INDEX (status, sort_order)

---

### 2.12 练习计划表 (practice_plans)

存储用户的个性化练习计划。

| 字段名 | 类型 | 长度 | 约束 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGSERIAL | - | PRIMARY KEY | 计划ID |
| user_id | BIGINT | - | NOT NULL, FOREIGN KEY | 用户ID |
| job_position | VARCHAR | 50 | NOT NULL | 岗位代码 |
| title | VARCHAR | 200 | NOT NULL | 计划标题 |
| description | TEXT | - | | 计划描述 |
| plan_data | JSONB | - | NOT NULL | 计划详情（JSON） |
| start_date | DATE | - | NOT NULL | 开始日期 |
| end_date | DATE | - | NOT NULL | 结束日期 |
| status | VARCHAR | 20 | DEFAULT 'active' | 状态：active/completed/cancelled |
| progress | INTEGER | - | DEFAULT 0 | 进度百分比 |
| created_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- PRIMARY KEY (id)
- INDEX (user_id, status)
- INDEX (job_position)

---

### 2.13 错题记录表 (mistake_records)

存储用户的错题记录。

| 字段名 | 类型 | 长度 | 约束 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGSERIAL | - | PRIMARY KEY | 记录ID |
| user_id | BIGINT | - | NOT NULL, FOREIGN KEY | 用户ID |
| job_position | VARCHAR | 50 | NOT NULL | 岗位代码 |
| question_content | TEXT | - | NOT NULL | 问题内容 |
| correct_answer | TEXT | - | | 正确答案 |
| user_answer | TEXT | - | | 用户回答 |
| issues | JSONB | - | | 问题点（JSON数组） |
| source_report_id | BIGINT | - | FOREIGN KEY | 来源报告ID |
| review_count | INTEGER | - | DEFAULT 0 | 复习次数 |
| last_review_at | TIMESTAMP | - | | 最后复习时间 |
| notes | TEXT | - | | 复习笔记 |
| created_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**：
- PRIMARY KEY (id)
- INDEX (user_id, job_position)
- INDEX (source_report_id)

---

### 2.14 帮助文章表 (help_articles)

存储帮助中心的文章内容。

| 字段名 | 类型 | 长度 | 约束 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| id | BIGSERIAL | - | PRIMARY KEY | 文章ID |
| category | VARCHAR | 50 | NOT NULL | 分类：tutorial/tips/faq |
| title | VARCHAR | 200 | NOT NULL | 文章标题 |
| summary | VARCHAR | 500 | | 文章摘要 |
| content | TEXT | - | NOT NULL | 文章内容（Markdown） |
| sort_order | INTEGER | - | DEFAULT 0 | 排序序号 |
| status | SMALLINT | - | DEFAULT 1 | 状态：0-禁用，1-启用 |
| created_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- PRIMARY KEY (id)
- INDEX (category, sort_order)
- INDEX (status)

---

## 三、外键关系

```sql
-- 面试会话 → 用户
ALTER TABLE interview_sessions
ADD CONSTRAINT fk_session_user
FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE;

-- 对话记录 → 面试会话
ALTER TABLE conversation_messages
ADD CONSTRAINT fk_message_session
FOREIGN KEY (session_id) REFERENCES interview_sessions(id) ON DELETE CASCADE;

-- 评估报告 → 面试会话
ALTER TABLE evaluation_reports
ADD CONSTRAINT fk_report_session
FOREIGN KEY (session_id) REFERENCES interview_sessions(id) ON DELETE CASCADE;

-- 评估报告 → 用户
ALTER TABLE evaluation_reports
ADD CONSTRAINT fk_report_user
FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE;

-- 逐题评价 → 评估报告
ALTER TABLE question_evaluations
ADD CONSTRAINT fk_evaluation_report
FOREIGN KEY (report_id) REFERENCES evaluation_reports(id) ON DELETE CASCADE;

-- 逐题评价 → 对话消息（问题）
ALTER TABLE question_evaluations
ADD CONSTRAINT fk_evaluation_question
FOREIGN KEY (question_message_id) REFERENCES conversation_messages(id) ON DELETE CASCADE;

-- 逐题评价 → 对话消息（回答）
ALTER TABLE question_evaluations
ADD CONSTRAINT fk_evaluation_answer
FOREIGN KEY (answer_message_id) REFERENCES conversation_messages(id) ON DELETE CASCADE;

-- 知识库切片 → 知识库文档
ALTER TABLE knowledge_chunks
ADD CONSTRAINT fk_chunk_document
FOREIGN KEY (document_id) REFERENCES knowledge_documents(id) ON DELETE CASCADE;

-- 能力成长记录 → 用户
ALTER TABLE ability_growth_records
ADD CONSTRAINT fk_growth_user
FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE;

-- 能力成长记录 → 评估报告
ALTER TABLE ability_growth_records
ADD CONSTRAINT fk_growth_report
FOREIGN KEY (report_id) REFERENCES evaluation_reports(id) ON DELETE CASCADE;

-- 推荐练习 → 用户
ALTER TABLE practice_recommendations
ADD CONSTRAINT fk_recommendation_user
FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE;

-- 推荐练习 → 评估报告
ALTER TABLE practice_recommendations
ADD CONSTRAINT fk_recommendation_report
FOREIGN KEY (report_id) REFERENCES evaluation_reports(id) ON DELETE CASCADE;

-- 练习计划 → 用户
ALTER TABLE practice_plans
ADD CONSTRAINT fk_plan_user
FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE;

-- 错题记录 → 用户
ALTER TABLE mistake_records
ADD CONSTRAINT fk_mistake_user
FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE;

-- 错题记录 → 评估报告
ALTER TABLE mistake_records
ADD CONSTRAINT fk_mistake_report
FOREIGN KEY (source_report_id) REFERENCES evaluation_reports(id) ON DELETE SET NULL;
```

---

## 四、PostgreSQL 特有功能应用

### 4.1 JSONB 类型

PostgreSQL 的 JSONB 类型用于存储结构化数据：

```sql
-- strengths, weaknesses, suggestions 字段使用 JSONB
-- 示例数据：
{
  "strengths": ["技术基础扎实", "表达流畅"],
  "weaknesses": ["分布式系统理解不足"],
  "suggestions": ["深入学习 Redis", "准备更多项目案例"]
}
```

### 4.2 自动更新时间戳

使用触发器自动更新 `updated_at` 字段：

```sql
-- 创建更新时间戳函数
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = CURRENT_TIMESTAMP;
    RETURN NEW;
END;
$$ language 'plpgsql';

-- 为需要的表创建触发器
CREATE TRIGGER update_users_updated_at BEFORE UPDATE ON users
FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_interview_sessions_updated_at BEFORE UPDATE ON interview_sessions
FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_knowledge_documents_updated_at BEFORE UPDATE ON knowledge_documents
FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_system_configs_updated_at BEFORE UPDATE ON system_configs
FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
```

---

## 五、Redis 缓存设计

### 5.1 缓存键命名规范

```
user:info:{user_id}                    # 用户信息
user:token:blacklist:{token_jti}          # JWT Token 黑名单（登出时使用）
session:info:{session_id}              # 会话信息
session:messages:{session_id}          # 会话消息列表
knowledge:search:{query_hash}          # 知识库检索结果
rate_limit:api:{user_id}:{endpoint}    # API 限流
```

### 5.2 缓存过期时间

| 缓存类型 | 过期时间 | 说明 |
| :--- | :--- | :--- |
| 用户信息 | 30分钟 | 用户登录后缓存 |
| JWT Token 黑名单 | 24小时 | Token 有效期（登出时添加到黑名单） |
| 会话信息 | 1小时 | 面试进行中缓存 |
| 知识库检索 | 24小时 | 检索结果缓存 |
| API 限流 | 1分钟 | 滑动窗口限流 |

---

## 六、向量数据库设计 (Milvus)

### 6.1 Collection 设计

**Collection 名称**：`knowledge_vectors`

**Schema**：
```python
{
    "fields": [
        {
            "name": "id",
            "type": "INT64",
            "is_primary": True,
            "auto_id": False
        },
        {
            "name": "chunk_id",
            "type": "INT64"
        },
        {
            "name": "document_id",
            "type": "INT64"
        },
        {
            "name": "job_position",
            "type": "VARCHAR",
            "max_length": 50
        },
        {
            "name": "category",
            "type": "VARCHAR",
            "max_length": 50
        },
        {
            "name": "content",
            "type": "VARCHAR",
            "max_length": 2000
        },
        {
            "name": "embedding",
            "type": "FLOAT_VECTOR",
            "dim": 1536  # text-embedding-3 维度
        }
    ]
}
```

### 6.2 索引配置

```python
index_params = {
    "metric_type": "COSINE",  # 余弦相似度
    "index_type": "IVF_FLAT",
    "params": {"nlist": 1024}
}
```

---

## 七、数据库初始化脚本

完整的建表脚本已保存在 `/doc/sql/schema.sql`

---

## 八、数据字典

### 8.1 枚举值定义

**job_status（求职阶段）**：
- `preparing`：准备中
- `interviewing`：面试中
- `offered`：已获 Offer

**job_position（面试岗位）**：
- `java_backend`：Java 后端开发
- `web_frontend`：Web 前端开发

**interview_mode（面试模式）**：
- `basic`：基础模式
- `full`：全真模式
- `special`：专项训练

**session_status（会话状态）**：
- `pending`：待开始
- `in_progress`：进行中
- `completed`：已完成
- `interrupted`：已中断

**stage（面试阶段）**：
- `self_intro`：自我介绍
- `tech`：技术提问
- `project`：项目提问
- `scenario`：场景题
- `behavior`：行为题

**grade（评级）**：
- `S`：优秀（90-100分）
- `A`：良好（80-89分）
- `B`：中等（70-79分）
- `C`：及格（60-69分）
- `D`：不及格（0-59分）

**practice_plan_status（练习计划状态）**：
- `active`：进行中
- `completed`：已完成
- `cancelled`：已取消

**help_article_category（帮助文章分类）**：
- `tutorial`：使用教程
- `tips`：面试技巧
- `faq`：常见问题

---

## 九、性能优化建议

### 9.1 索引优化
- 为高频查询字段添加索引
- 使用复合索引优化多条件查询
- 定期分析慢查询日志

### 9.2 分区策略
- 对话记录表按月分区（PARTITION BY RANGE）
- 评估报告表按年分区

### 9.3 归档策略
- 超过 1 年的对话记录归档至历史表
- 超过 2 年的数据迁移至冷存储

---

> **提示**：本数据库设计支持未来扩展，如增加新岗位、新评估维度等。
